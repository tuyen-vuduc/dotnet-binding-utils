name: Android Binding Libraries - PayPal
run-name: ${{ inputs.LIB_ARTIFACT }} ��� NuGet

on:
  workflow_dispatch:
    inputs:
      LIB_ARTIFACT:
        description: 'Android library to publish'
        required: true
        type: choice
        options:
        - com.paypal.android:card-payments:1.7.1
        - com.paypal.android:paypal-web-payments:1.7.1
        - com.paypal.android:fraud-protection:1.7.1
        - com.paypal.android:payment-buttons:1.7.1

jobs:
  build-then-publish:
    uses: ./.github/workflows/reusable-android-build.yml
    with:
      artifact: ${{ inputs.LIB_ARTIFACT }}
      dotnet-version: '9.0'
      java-version: '21'
      requires-mapbox-token: false
      requires-drivesmart-credentials: false
    secrets:
      NUGET_PUSH_API_KEY: ${{ secrets.NUGET_PUSH_API_KEY }}
